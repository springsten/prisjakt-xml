export interface IProduct {
  id: number;
  title: string;
  price: number;
  stockLevel: number;
  categoryId: string;
  popularityFactor: number;
  pimId: string;
  active: boolean;
  description: string;
  color: string;
}
